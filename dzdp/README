大众点评爬虫css加密反爬

目标页面部分文本在页面源代码里查看到的是一个有特定class name的html标签，标签里没有任何内容
分析发现，这一类标签对应的页面文本是从一个svg文件里匹配到的，
svg文件的特点：
多行文本组成的一个文件，每一行文本都包括在一个svg标签内，svg标签有一个y坐标值属性
如何从svg文件里匹配文本
目标页面里的特殊标签对应的class都有background属性，通过background的x, y属性计算出文字在svg文件中对应的行号和列号，即可匹配
行号计算方法：
如果|background[y]|值小于svg页面第一行的y值，则该文本的行号对应第一行
如果|background[y]|值大于svg页面第n行的y值并且小于第n+1行的y值，则该文本的行号对应第n+1行
列号计算方法：
|background[x]| / 14 + 1

将页面源代码里的特殊标签替换为匹配到的文本即可提取全部内容

主要技术应用： 正则表达式